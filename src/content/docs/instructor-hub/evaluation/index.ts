import type { DocGroup } from "@/types/docs";
import { article as a0 } from "./creer-un-quiz";
import { article as a1 } from "./attestations";

export const group: DocGroup = {
  id: "evaluation",
  title: "Évaluation",
  articles: [a0, a1],
};

export default group;
