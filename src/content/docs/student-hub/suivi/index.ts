import type { DocGroup } from "@/types/docs";
import { article as a0 } from "./acceder-aux-supports";
import { article as a1 } from "./rendre-un-devoir";

export const group: DocGroup = {
  id: "suivi",
  title: "Suivi des cours",
  articles: [a0, a1],
};

export default group;
