import type { DocGroup } from "@/types/docs";
import { article as a0 } from "./sinscrire-a-une-formation";

export const group: DocGroup = {
  id: "inscription",
  title: "Inscription",
  articles: [a0],
};

export default group;
