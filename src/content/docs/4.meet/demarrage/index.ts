import type { DocGroup } from "@/types/docs";
import { article as a0 } from "./premiere-connexion";
import { article as a1 } from "./raccourcis-clavier";

export const group: DocGroup = {
  id: "demarrage",
  title: "Démarrage",
  articles: [a0, a1],
};

export default group;
