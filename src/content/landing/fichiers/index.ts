import type { DocGroup } from "@/types/docs";
import { article as a0 } from "./preparer-ses-fichiers";
import { article as a1 } from "./formats-supportes";

export const group: DocGroup = {
  id: "fichiers",
  title: "Préparation des fichiers",
  articles: [a0, a1],
};

export default group;
