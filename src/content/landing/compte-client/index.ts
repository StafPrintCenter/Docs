import type { DocGroup } from "@/types/docs";
import { article as a0 } from "./gerer-son-compte";

export const group: DocGroup = {
  id: "compte-client",
  title: "Compte client",
  articles: [a0],
};

export default group;
